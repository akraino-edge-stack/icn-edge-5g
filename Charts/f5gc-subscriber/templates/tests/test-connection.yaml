apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "f5gc-subscriber.fullname" . }}-test-connection"
  labels:
    {{- include "f5gc-subscriber.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "f5gc-subscriber.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
